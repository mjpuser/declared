# Declared

Setup

```bash
# ~/.bashrc

# This allows you to use docker as a dev environment
export UID=${UID}
export GID=${GID}
```

Startup

```bash
# brings you in a docker file for your dev env
yarn dev
```
